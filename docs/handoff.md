# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Запусти проект, убедись что он живой:
   ```bash
   npm install && npm run check
   ```
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-09-10
**В одном предложении:** Комплект документов ПДн от юрфирмы для ООО «ЕС Транс» изучен полностью и сопоставлен с фактическим состоянием сайта es-trans.ru — сайт уже частично актуализирован, остались организационные шаги (заполнить приказы, уведомить РКН), ждём решения владельца компании.
**Что работает прямо сейчас:** git + GitHub (krutko77/security-and-personal-data, приватный), сайт es-trans.ru — политики и согласия актуальны, формы с рабочим чекбоксом согласия, cookie-баннер работает.
**Что в процессе / недоделано:** Приказ о назначении ответственного за ПДн и приказ о перечне допущенных лиц — не заполнены ФИО/должности (ждём решение пользователя). Уведомление в Роскомнадзор о начале обработки ПДн, судя по документам, ещё не подано.
**Где живёт прод (URL / SERVER_ID):** Сайт компании — https://es-trans.ru/ (прод, уже живой, не деплоится этим репозиторием). Шаблон Б24-дашборда (`src/`) — не задеплоен, отдельная история, см. предыдущие записи changelog.md.
**Главные подводные камни:** zip от юрфирмы — имена файлов в кодировке cp866, нужна перекодировка cp437→cp866 при распаковке. python-docx не установлен — текст docx читать через XML `word/document.xml`. WebFetch не работает для es-trans.ru — использовать curl напрямую. Все распакованные файлы в /tmp/ временные, не переживают сессию.
**Первое, что стоит сделать новому исполнителю:** Прочитай docs/last-session.md, затем память агента (`~/.claude/projects/.../memory/es_trans_pdn_doc_package.md` и `company_es_trans.md`) — там полная сводка аудита и список организационных шагов. Проверь, ответил ли пользователь на вопрос про ответственное лицо за ПДн.
